# 쭌 리서치 어시스턴트 — 배포 가이드

## 이게 뭔가요?

이 프로젝트는 **웹사이트**입니다. 지금은 홍준님 컴퓨터 안에 코드만 있는 상태이고,
이걸 인터넷에 올려야(= "배포") 브라우저에서 접속할 수 있습니다.

**Vercel**이라는 서비스가 이 코드를 인터넷에 올려주는 역할을 합니다. 무료입니다.

---

## 필요한 것 (총 3개)

1. **GitHub 계정** — 코드를 저장하는 곳 (무료)
2. **Vercel 계정** — 코드를 웹사이트로 만들어주는 곳 (무료)
3. **Node.js** — 컴퓨터에서 코드를 실행하는 프로그램 (무료)

---

## Step 1: Node.js 설치

1. https://nodejs.org 접속
2. **LTS** 버전 다운로드 (왼쪽 초록 버튼)
3. 설치 파일 실행 → "다음" 계속 클릭
4. 설치 완료 확인:
   - Windows: 시작메뉴 → "cmd" 검색 → 명령 프롬프트 열기
   - 타이핑: `node --version`
   - `v20.x.x` 같은 숫자가 나오면 성공

---

## Step 2: GitHub 가입 + 코드 올리기

### 2-1. GitHub 가입
1. https://github.com 접속
2. "Sign up" 클릭
3. 이메일, 비밀번호, 사용자명 입력
4. 이메일 인증 완료

### 2-2. 새 저장소(Repository) 만들기
1. GitHub 로그인 후 오른쪽 상단 "+" → "New repository"
2. Repository name: `jjun-research`
3. "Public" 선택
4. "Create repository" 클릭

### 2-3. 코드 업로드
**방법 A (쉬운 방법 — 드래그앤드롭):**
1. GitHub에서 만든 저장소 페이지로 이동
2. "uploading an existing file" 링크 클릭
3. `쭌-리서치` 폴더 안의 **모든 파일과 폴더**를 드래그앤드롭
4. "Commit changes" 클릭

**방법 B (명령어 — 더 정확):**
명령 프롬프트(cmd)에서:
```
cd "쭌-리서치 폴더 경로"
npm install
git init
git add .
git commit -m "쭌 리서치 어시스턴트 초기 코드"
git branch -M main
git remote add origin https://github.com/본인아이디/jjun-research.git
git push -u origin main
```

---

## Step 3: Vercel 배포 (가장 중요!)

1. https://vercel.com 접속
2. "Sign Up" → "Continue with GitHub" 선택 (GitHub 계정으로 가입)
3. GitHub 연동 허용

### 배포하기
4. "New Project" 클릭
5. 목록에서 `jjun-research` 저장소 선택 → "Import"
6. 설정은 건드리지 말고 그대로 "Deploy" 클릭
7. 1-2분 기다리면 **배포 완료!**
8. `https://jjun-research.vercel.app` 같은 주소가 생깁니다

### 끝! 이제 이 주소로 접속하면 쭌 리서치가 열립니다.

---

## 자동 업데이트 원리

배포가 완료되면 이런 것들이 **자동으로** 돌아갑니다:

| 기능 | 주기 | 원리 |
|------|------|------|
| 지수/주가 시세 | 5분마다 | Yahoo Finance API 자동 호출 |
| 골든크로스 감지 | 1시간마다 | 3개월 데이터로 이평선 자동 계산 |
| 증권사 리포트 | 30분마다 | 네이버 리서치 자동 크롤링 |
| 차트 | 실시간 | TradingView 위젯 (자체 실시간) |

---

## 프로젝트 구조 설명

```
쭌-리서치/
├── package.json          ← 프로젝트 설정 파일
├── next.config.js        ← Next.js 설정
├── vercel.json           ← Vercel 배포 설정 (Cron 스케줄)
├── app/
│   ├── layout.js         ← 전체 레이아웃 (HTML 뼈대)
│   ├── page.js           ← 메인 페이지 (화면에 보이는 것)
│   ├── globals.css       ← 디자인/스타일
│   └── api/
│       ├── market/route.js       ← 시세 API (Yahoo Finance)
│       ├── golden-cross/route.js ← 골든크로스 계산 API
│       └── reports/route.js      ← 리포트 크롤링 API
└── data/                 ← 나중에 쭌 코멘트용 데이터
```

---

## 문제가 생기면?

| 문제 | 해결 |
|------|------|
| 시세가 안 나와요 | Yahoo Finance가 일시적으로 막힐 수 있음. 잠시 후 재시도 |
| 리포트가 비어있어요 | 네이버 리서치 페이지 구조가 바뀌면 크롤링이 깨질 수 있음 |
| 차트가 안 떠요 | TradingView 스크립트 로딩 문제. 새로고침 |
| 배포가 실패해요 | Vercel 대시보드에서 "Logs" 확인 → 에러 메시지 캡처해서 쭌에게 물어보기 |

---

## 향후 업그레이드 아이디어

- **한국투자증권 Open API** 연동 → 더 정확한 실시간 시세
- **Claude API** 연동 → 매일 자동 시황 코멘트 + 쭌 Pick 생성
- **KRX 상한가 API** → 상한가 종목 자동 수집
- **알림 기능** → 골든크로스 발생 시 텔레그램/카카오톡 알림
